//api

// backend url - hardcoded for now
// const BASE_URL = 'http://localhost:5000';

// export const getUserData = async () => {
//     const response = await fetch(`${BASE_URL} + /api/user`);
//     return response.json();
// }
